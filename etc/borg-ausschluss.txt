# Spielinstallationen gehoeren NICHT in die Sicherung - SteamCMD laedt sie
# jederzeit neu (Enshrouded allein: 8,9 GB). Gesichert werden Spielstaende,
# Serverkonfiguration und die compose-Dateien.
sh:/srv/games/*/server/*.dat
sh:/srv/games/*/server/*.exe
sh:/srv/games/*/server/*.kfc*
sh:/srv/games/*/server/*.pdb
sh:/srv/games/*/server/steamapps
sh:/srv/games/*/server/logs
sh:/srv/games/*/.steam
sh:/srv/games/*/steamcmd
sh:/srv/games/*/current_version
# Palworld (Image thijsvanloef): Installation und SteamCMD bleiben draussen,
# gesichert werden Pal/Saved (Welt, Spielerstaende, Konfiguration) und backups/.
sh:/srv/games/palworld/Pal/Binaries
sh:/srv/games/palworld/Pal/Content
sh:/srv/games/palworld/Pal/Plugins
sh:/srv/games/palworld/Engine
sh:/srv/games/palworld/steamapps
sh:/srv/games/palworld/PalServer.sh
sh:/srv/games/palworld/*.so
sh:/srv/games/palworld/steamclient*
# Satisfactory: Installation raus, "saved" und "backups" bleiben drin.
sh:/srv/games/satisfactory/gamefiles
sh:/srv/games/satisfactory/logs
# FOUNDRY: Installation raus, "data" (Spielstaende) bleibt.
sh:/srv/games/foundry/server
# Windrose: alles unter server/ ist Installation AUSSER R5/Saved und
# R5/ServerDescription.json — deshalb gezielt die dicken Brocken ausschliessen
# statt "server" pauschal, sonst faellt der Spielstand mit heraus.
sh:/srv/games/windrose/server/steamapps
sh:/srv/games/windrose/server/Engine
sh:/srv/games/windrose/server/R5/Content
sh:/srv/games/windrose/server/R5/Binaries
sh:/srv/games/windrose/server/R5/Plugins
sh:/srv/games/windrose/server/R5/.sentry-native
# Ab hier haengt spiel-verwalten Bloecke der Form
#   # >>> panel:<spiel> ... # <<< panel:<spiel>
# an, sobald ein Spiel aus dem Katalog installiert wird. Diese Bloecke gehoeren
# NICHT ins Repositorium: sie beschreiben, was auf DIESER Maschine gerade
# installiert ist, nicht den Bauplan. werkzeuge/abgleich.sh blendet sie
# deshalb beim Vergleich aus.
# *From here on spiel-verwalten appends per-game blocks as games are installed.
#  They describe what is installed on this machine, not the blueprint, and are
#  therefore excluded from the repository and ignored by the comparison tool.*
